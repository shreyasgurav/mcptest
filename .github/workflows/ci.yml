# Example GitHub Actions workflow for testing MCP servers with mcptest
name: MCP Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-mcp:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Test MCP Server
        run: node dist/cli.js run examples/

      - name: Validate MCP Server
        run: node dist/cli.js validate --config examples/echo.mcptest.yaml
